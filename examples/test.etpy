use bof;

fn go(args: char*, len: int) -> void {
    var snap: u64 = (u64)Kernel32.CreateToolhelp32Snapshot(TH32CS_SNAPPROCESS, 0);
    if snap == (u64)0xFFFFFFFFFFFFFFFF {
        BeaconPrintf(0, "[!] fail\n");
        ret;
    }
    ret;
}
